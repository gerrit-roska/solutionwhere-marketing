# Secrets never land in git — env NAMES live in .env.example, values live only here.
.env
.env.*
!.env.example

# Run artifacts
runs/
dry-run-output/
node_modules/

# Generated dashboard artifacts (rebuilt by report runs)
dashboard/data.json
dashboard/report.html
